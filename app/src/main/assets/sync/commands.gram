#JSGF V1.0;

grammar com.musicvoice.commands;

<command> = (resume playback)	|
		  (pause music)			|
		  (skip to next song)	|
		  (shuffle play)        |
		  (play previous song)	;

public <commands> = <command>	;